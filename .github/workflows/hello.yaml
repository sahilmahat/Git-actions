name: hello

on: 
  push:
    branches: [main]

jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - name: say hello to user
        run: echo "say hello dosto"
      - name: hello to user
        run: echo "shello $USER"
  details:
    runs-on: ubuntu-latest
    steps:
      - name: what is name of docker pythons
        run: docker --version && python --version
